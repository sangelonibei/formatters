export interface CourseDTO {
  title: string;
  shortDescription: string;
  startDate: string;
  duration: number;
  mode: string;
  location: string;
  category: string;
  subCategory: string;
  requirements: string[];
}
